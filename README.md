# Лендинг Оксаны Юрченко

HR-консалтинг и подбор персонала для гастроиндустрии Аргентины.
Русская и испанская версии, статический сайт без сборки и без зависимостей.

- Русская версия — `index.html`
- Испанская версия — `es/index.html`
- Что осталось уточнить — [`БРИФ.md`](БРИФ.md)
- Какие нужны фотографии — [`assets/img/README.md`](assets/img/README.md)

---

## Как опубликовать на GitHub Pages

1. Откройте репозиторий на GitHub → вкладка **Settings**.
2. В левом меню — **Pages**.
3. В блоке *Build and deployment*, поле *Source*, выберите **Deploy from a branch**.
4. Ниже выберите ветку (`main`, если изменения уже влиты, иначе
   `claude/oxana-personal-landing-e7uvxk`) и папку **`/ (root)`**. Нажмите **Save**.
5. Через 1–2 минуты обновите страницу — вверху появится адрес вида
   `https://oxana-arg.github.io/sitebar/`.

### Свой домен

Купите домен, добавьте в корень репозитория файл `CNAME` с одной строкой — вашим доменом
(например `oksanahr.com.ar`), а у регистратора пропишите записи, которые GitHub покажет
в Settings → Pages. После этого в `index.html`, `es/index.html` и `sitemap.xml` нужно
заменить `https://oxana-arg.github.io/sitebar/` на новый адрес — это девять строк,
обычным поиском по файлу.

---

## Как менять тексты

Тексты лежат прямо в HTML — редактировать можно на самом GitHub, ничего не устанавливая:
откройте файл, нажмите карандаш, поправьте, нажмите **Commit changes**. Через минуту
правка появится на сайте.

Важно: **одну и ту же правку нужно внести в оба файла** — `index.html` (русский) и
`es/index.html` (испанский). Структура блоков в них одинаковая.

### Контакты

Во всех кнопках сейчас стоят заглушки. Найдите поиском по файлу и замените — в обоих файлах:

| Найти | Заменить на |
|---|---|
| `https://wa.me/0000000000` | ваш номер: только цифры, без `+` и пробелов |
| `https://t.me/username` | `https://t.me/ваш_ник` |
| `hola@example.com` | вашу почту |

---

## Как поменять палитру

Собраны три пастельных варианта: `mate` (песочно-шалфейный), `rosa` (пудрово-винный),
`porteno` (лавандово-мятный).

**Посмотреть вживую:** добавьте `?themes=1` к адресу страницы — в правом нижнем углу
появятся три кружка. Переключатель виден только с этим параметром, обычные посетители
его не увидят.

**Закрепить выбранную:** в `index.html` и `es/index.html` в самой первой строке после
`<!doctype html>` поменяйте значение:

```html
<html lang="ru" data-theme="mate">   <!-- было -->
<html lang="ru" data-theme="rosa">   <!-- стало -->
```

Все цвета описаны в начале `assets/css/styles.css`, в разделе «Токены палитр».

---

## Как посмотреть локально

Файлы нужно открывать через веб-сервер, а не двойным кликом — иначе браузер
заблокирует загрузку шрифтов.

```bash
python3 -m http.server 8000
```

Дальше открыть `http://localhost:8000` (русская версия) и `http://localhost:8000/es/`.

---

## Что внутри

```
index.html              русская версия
es/index.html           испанская версия
assets/css/styles.css   вся вёрстка, три палитры, подключение шрифтов
assets/js/main.js       меню, появление блоков, переключатель палитр (~2 КБ)
assets/fonts/           Prata и Golos Text, лицензия SIL OFL 1.1
assets/img/             заглушки под фотографии, фавикон, обложка для мессенджеров
.nojekyll               чтобы GitHub Pages отдавал файлы как есть
robots.txt, sitemap.xml для поисковиков
```

Шрифты лежат в репозитории, а не подгружаются с Google Fonts: страница открывается
быстрее, работает без внешних запросов и не зависит от доступности чужого сервера.
Оба шрифта распространяются по SIL Open Font License 1.1 — тексты лицензий в
`assets/fonts/`.
